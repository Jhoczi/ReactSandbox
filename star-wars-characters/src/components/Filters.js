import React, {Component, useState} from 'react';
import PropTypes from 'prop-types';

const Filters = ({characters, filterCharacters}) => {

    return (
        <div>

        </div>
    );
}

Filters.propTypes = {};

export default Filters;
